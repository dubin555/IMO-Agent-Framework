# .env.example
#
# Copy this file to .env and fill in your API keys.
# This file can be safely committed to version control.

# --- Azure OpenAI Configuration ---
# Used when specifying a model like "azure/your-deployment-name"
AZURE_OPENAI_API_KEY="your-azure-api-key"
AZURE_OPENAI_ENDPOINT="https://your-resource-name.openai.azure.com/"
AZURE_OPENAI_API_VERSION="your-api-version"

# --- Google Gemini Configuration ---
# Used when specifying a model like "gemini/gemini-1.5-pro-latest"
GOOGLE_API_KEY="your-google-gemini-api-key"

# --- Anthropic Claude Configuration ---
# Used when specifying a model like "claude-3-opus-20240229"
ANTHROPIC_API_KEY="your-anthropic-claude-api-key"

# --- OpenAI Configuration (if not using Azure) ---
# Used when specifying a model like "gpt-4o"
# OPENAI_API_KEY="your-openai-api-key"